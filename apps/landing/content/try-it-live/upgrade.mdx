import { VaultBar, DemoCards, UpgradePanel, TxAnatomyStrip } from "@/components/try-it-live"

<VaultBar />

# Program upgrade

The upgrade authority has been transferred to a Trana Authority PDA. The wallet key alone cannot patch this program.

<DemoCards active="upgrade" />

<UpgradePanel />

<TxAnatomyStrip policy="Require" />
