import { SeedPhraseBanner, DemoCards, DepositPanel, TxAnatomyStrip } from "@/components/try-it-live"

# Deposit

Anyone can deposit into the shared vault. No passkey required.

<SeedPhraseBanner />

<DemoCards active="deposit" />

<DepositPanel />

<TxAnatomyStrip policy="None (open)" />
