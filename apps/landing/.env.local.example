# Trana landing app — devnet config
# Copy to .env.local and fill in real values

# Public key of the wallet that ran `node scripts/init-vault.mjs <vault-id>` on devnet.
# Pool PDA = [b"trana-pool", DEMO_VAULT_AUTHORITY]
# Leave blank while the demo pool is not yet deployed.
NEXT_PUBLIC_DEMO_VAULT_AUTHORITY=
